// Copy this file to runtime-config.js during deployment and supply real values.
// The anon key is safe for browser use; never put a service-role key here.
window.ENV = {
  API_BASE_URL: 'https://api.your-domain.example',
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'your-public-anon-key'
};
